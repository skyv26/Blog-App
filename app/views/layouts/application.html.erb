<!DOCTYPE html>
<html>
  <head>
    <title>BlogApp</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
   <p class="notice"><%= notice %></p>
   <p class="alert"><%= alert %></p>
   <% if user_signed_in? %>
    <p><b>Welcome, </b><span><%= current_user.name %></span></p>
    <%= button_to "logout", destroy_user_session_path, method: :delete, class: 'comment-btn'  %>
   <% end %>
    <%= yield %>
  </body>
</html>
